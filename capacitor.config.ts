import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appName: 'Hackathon',
  appId: 'fr.bodetrnite.Hackathon',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
